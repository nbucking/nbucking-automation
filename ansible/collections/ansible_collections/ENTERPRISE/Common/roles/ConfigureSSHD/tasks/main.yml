---
# tasks file for Deploy_sshd_config
- name: Copy sshd_config_master to remote server
  ansible.builtin.template:
    src: sshd_config_master
    dest: "{{ sshd_config }}"
    owner: root
    group: root
    mode: '0600'
    backup: yes
  notify: "restart sshd"
