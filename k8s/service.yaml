apiVersion: v1
kind: Service
metadata:
  name: nbucking-web
  namespace: default
  labels:
    app: nbucking-web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: nbucking-web
